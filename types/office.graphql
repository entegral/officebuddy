
type Office {
  guid: String!
  name: String!
  description: String
  admins: [User!]
  members: [User!]
  events: [Event!]
  schedules(scheduleGUID: String): [Schedule!]
}

extend type Query {
  office(officeGUID: String!): Office
  offices(userGUID: String!): [Office!]
}